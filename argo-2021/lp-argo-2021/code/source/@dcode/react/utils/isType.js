// Check if the element belongs to a video element
// only accept <source /> <track />
// <MyComponent isControlChild />
// const isControlChild(component) {
// 	if (component.props && component.props.isControlChild) {
// 		return true;
// 	}
// 	return component.type === 'source' || component.type === 'track';
// }
